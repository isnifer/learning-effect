# Product identity

Use **Red Docket** as the user-facing product name, `red-docket` as its machine-readable identifier,
and `reddocket.app` as its official domain. Do not use the former names Learning Effect or Threadmark.
